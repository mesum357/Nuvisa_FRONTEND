# Copy to .env.local

NEXT_PUBLIC_API_URL=http://localhost:4000
APP_ENV=development
NEXTAUTH_URL=http://localhost:3002

NEXT_PUBLIC_ADMIN_API_URL=http://localhost:3001
ADMIN_PUBLIC_URL=http://localhost:3001

MINDEE_API_KEY=
NEXT_PUBLIC_VISA_API_SERVER=https://api.qa.stampmyvisa.com
NEXT_PUBLIC_CLIENT_ID=

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# SENDGRID_API_KEY=
# SENDGRID_FROM=no-reply@nuvisa.com

# Supabase Postgres (required at runtime; needed for migrate deploy on Render pre-deploy)
DATABASE_URL=
DIRECT_URL=

# --- Render deploy ---
# Build: npm run build (generate + next build only — no DB during compile)
# Pre-deploy command: npx prisma migrate deploy
# Set DATABASE_URL, DIRECT_URL, NEXTAUTH_URL, NEXT_PUBLIC_* in Render → Environment
